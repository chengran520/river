var WEB_ROOT = "/gxhy";
var count = {
		loadPatrolEvent	: function(){
			var url = WEB_ROOT + '/routine/work/getEventCountByRiverId'
			var rs = commonUtils.get(url, null, true);
			if(rs.data.length > 0){
				
			}
		}
};
$(function(){
	count.loadPatrolEvent();
});